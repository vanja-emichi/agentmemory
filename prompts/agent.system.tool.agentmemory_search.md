## AgentMemory search
Use `agentmemory_search` when prior project context, decisions, lessons, or sessions may answer the current task.
args: `query` (required), optional `limit` (default 10)
Only report memories returned by the tool. If there are no matches, say so instead of guessing.
